# Core ML / pipeline
torch
torchvision
ultralytics
opencv-python-headless
numpy
scipy
pyyaml
certifi

# OCR — EasyOCR works everywhere; PaddleOCR (more accurate on plates) needs Py<=3.12
easyocr
paddleocr ; python_version < "3.13"
paddlepaddle ; python_version < "3.13"

# Web app
fastapi
uvicorn[standard]
python-multipart
yt-dlp
reportlab        # PDF report export
# NOTE: ffmpeg (system package) is needed to encode the H.264 annotated video.
#   macOS: brew install ffmpeg   |   Ubuntu: sudo apt install ffmpeg
# Without it, the annotated video falls back to mp4v (may not play in all browsers).
